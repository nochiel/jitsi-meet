// FIXME(nochiel) Should a wallet be a participant? Why?
// A participant is "visible" to all other participants.
/**
 * Fixed name of the wallet fake participant.
 */
export const WALLET_PARTICIPANT_NAME = 'wallet';

/**
 * wallet ID.
 */
export const WALLET_ID = 'wallet';

/**
 * wallet UI Options.
 */
export const WALLET_UI_OPTIONS = {
};
